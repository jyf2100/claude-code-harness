---
name: ci-analyze-failures
description: "分析 CI 失败日志，确定原因。当 CI/CD 流水线中测试或构建失败时使用。"
allowed-tools: ["Read", "Grep", "Bash"]
---

# CI Analyze Failures

分析 CI/CD 流水线失败并确定原因的技能。
解读 GitHub Actions、GitLab CI 等的日志。

---

## 输入

- **CI 日志**: 失败作业的日志
- **run_id**: CI 运行的标识符（如有）
- **仓库上下文**: CI 配置文件

---

## 输出

- **失败原因的确定**: 具体原因
- **修复建议**: 解决方法的提案

---

## 执行步骤

### Step 1: 确认 CI 状态

```bash
# GitHub Actions 的情况
gh run list --limit 5

# 确认最近的失败
gh run view --log-failed
```

### Step 2: 获取失败日志

```bash
# 特定 run 的日志
gh run view {{run_id}} --log

# 仅失败步骤
gh run view {{run_id}} --log-failed
```

### Step 3: 分析错误模式

#### 构建错误

```
模式: "error TS\d+:" 或 "Build failed"
可能原因:
- TypeScript 类型错误
- 依赖缺失
- 语法错误
```

#### 测试错误

```
模式: "FAIL" 或 "✕" 或 "AssertionError"
可能原因:
- 测试失败
- 测试超时
- Mock 不匹配
```

#### 依赖错误

```
模式: "npm ERR!" 或 "Could not resolve"
可能原因:
- package.json 不一致
- 私有包认证
- 版本冲突
```

#### 环境错误

```
模式: "not found" 或 "undefined"
可能原因:
- 环境变量未设置
- Secret 缺失
- 路径问题
```

### Step 4: 输出分析结果

```markdown
## 🔍 CI 失败分析

**Run ID**: {{run_id}}
**失败时间**: {{timestamp}}
**失败步骤**: {{step_name}}

### 原因确定

**错误类型**: {{构建 / 测试 / 依赖 / 环境}}

**错误消息**:
```
{{错误的核心部分}}
```

**原因分析**:
{{具体原因的说明}}

### 相关文件

| 文件 | 相关性 |
|-----|-------|
| `{{path}}` | {{相关内容}} |

### 修复建议

1. {{具体的修复步骤1}}
2. {{具体的修复步骤2}}

### 自动修复的可能性

- 自动修复: {{可能 / 不可能}}
- 理由: {{理由}}
```

---

## 错误模式字典

### TypeScript 错误

| 错误码 | 含义 | 典型修复 |
|-------|------|---------|
| TS2304 | 找不到名称 | 添加 import |
| TS2322 | 类型不匹配 | 修复类型 |
| TS2345 | 参数类型错误 | 修正参数 |
| TS7006 | 隐式 any | 添加类型注解 |

### npm 错误

| 错误 | 含义 | 典型修复 |
|-----|------|---------|
| ERESOLVE | 依赖解析失败 | 删除 package-lock 并重新安装 |
| ENOENT | 找不到文件 | 确认路径 |
| EACCES | 权限错误 | 确认 CI 设置 |

### Jest/Vitest 错误

| 错误 | 含义 | 典型修复 |
|-----|------|---------|
| Timeout | 测试超时 | 延长超时或修复异步 |
| Snapshot | 快照不匹配 | `npm test -- -u` |

---

## 多个错误的优先级

1. **构建错误**: 最优先修复
2. **依赖错误**: 构建前必须解决
3. **测试错误**: 构建成功后处理
4. **Lint 错误**: 最后处理

---

## 连接到下一步操作

分析完成后:

> 📊 **分析完成**
>
> **原因**: {{原因摘要}}
>
> **下一步操作**:
> - 「修复」→ 尝试自动修复
> - 「详细」→ 更详细的分析
> - 「跳过」→ 切换到手动处理

---

## 注意事项

- **日志很大**: 提取重要部分
- **注意连锁错误**: 找到第一个错误
- **环境差异**: 考虑本地和 CI 的区别
