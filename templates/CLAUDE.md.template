---
_harness_template: "CLAUDE.md.template"
_harness_version: "2.5.27"
---

# CLAUDE.md - Claude Code 执行指南

> **项目**: {{PROJECT_NAME}}
> **创建日期**: {{DATE}}
> **语言**: {{LANGUAGE}}

---

## ⚠️ 首先阅读

**请务必首先参考 `AGENTS.md`。**

- 开发流程概览
- 角色分工
- 禁止事项

本文件仅记载 **Claude Code 固有的设置**。

---

## 1. Claude Code 的职责范围

### ✅ 负责的工作

- 4个文件以上、超过100行的实现
- 提交 & 推送
- CI 通过确认（失败时自动修复，最多3次）
- staging 部署（如适用）

### ❌ 禁止事项

- **禁止生产环境部署**（Cursor 的职责）
- 范围外的工作
- 安全设置的更改

---

## 2. 提交信息规范

```
feat: 新功能
fix: 错误修复
docs: 文档
refactor: 重构
test: 测试
chore: 其他
```

例: `feat: 添加用户认证功能`

---

## 3. CI 失败时的处理

### 处理流程

1. 检测 CI 失败
2. 确认错误日志
3. 修复 → 重新提交 → 重新运行 CI
4. **失败3次后上报给 Cursor**

### 上报报告格式

```markdown
## ⚠️ CI失败上报

**失败次数**: 3次
**分支**: {{branch}}

### 错误内容
[错误日志摘要]

### 尝试的修复
1. [修复1]
2. [修复2]
3. [修复3]

### 推测原因
[原因推测]

### 对 Cursor 的请求
[具体的请求内容]
```

---

## 4. 交接协议

### Cursor → Claude Code（工作委托）

```markdown
## 工作委托
- **任务概要**: 具体的工作内容
- **范围**: 更改目标文件和范围
- **完成条件**: 明确的验收标准
```

### Claude Code → Cursor（工作完成报告）

使用 `/handoff-to-cursor` 命令，或按以下格式报告：

```markdown
## 🎯 工作完成报告

**报告日期**: YYYY-MM-DD HH:MM
**负责人**: Claude Code

### 完成的任务
- **任务名称**: [任务描述]

### 实施内容
1. [实施的工作1]
2. [实施的工作2]

### 更改文件
| 文件 | 更改内容 |
|---------|---------|
| `path/to/file1` | [更改概要] |

### CI 状态
- CI: ✅ 通过 / ❌ 失败

### 下一步操作（面向 Cursor）
1. [ ] 确认更改内容
2. [ ] 判断是否部署到生产环境
```

---

## 5. 会话例行操作

### 会话开始时

```bash
# 状态确认
git status -sb
cat Plans.md
head -50 AGENTS.md
```

或输入"会话开始"启动技能。

### 工作完成时

```bash
# 提交（遵循规范）
git add -A
git commit -m "feat: [更改内容]"
git push

# 完成报告
/handoff-to-cursor
```

---

## 6. 可用命令

| 命令 | 用途 |
|---------|------|
| `/sync-status` | 把握情况・提议下一步操作 |
| `/work` | 执行任务（开始时自动更新为 cc:WIP） |
| `/handoff-to-cursor` | 完成报告 |

---

## 7. 故障排除

| 症状 | 解决方案 |
|------|--------|
| 找不到任务 | 确认 Plans.md，或向 Cursor 请求 |
| CI 持续失败 | 最多修复3次，之后上报 |
| 范围不明确 | 向 Cursor 请求确认 |

---

*本文档请与 AGENTS.md 配合使用。*
