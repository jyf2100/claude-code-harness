---
_harness_template: "AGENTS.md.template"
_harness_version: "2.5.27"
---

# AGENTS.md - 开发流程概览

> **项目**: {{PROJECT_NAME}}
> **创建日期**: {{DATE}}
> **语言**: {{LANGUAGE}}

---

## 0. 开发流程概览

### 2代理体制

| 代理 | 角色 | 主要职责 |
|-------------|------|---------|
| **Cursor** | PM（项目管理员） | 任务管理、审查、生产部署判断 |
| **Claude Code** | Worker（执行者） | 实现、测试、CI修复、staging 部署 |

### 工作流

```
Cursor (PM)          Claude Code (Worker)
    │                       │
    │  1. 任务委托        │
    │  (/handoff-to-claude)      │
    │──────────────────────>│
    │                       │
    │                       │ 2. 把握情况→实现→提交
    │                       │    (/sync-status → /work)
    │                       │
    │  3. 完成报告          │
    │  (/handoff-to-cursor) │
    │<──────────────────────│
    │                       │
    │ 4. 审查・生产判断  │
    │  (/review-cc-work)    │
    │                       │
```

---

## 1. 文件构成

| 文件 | 用途 | 参考者 |
|---------|------|--------|
| `AGENTS.md` | 开发流程概览（本文件） | 双方 |
| `CLAUDE.md` | Claude Code 固有设置 | Claude Code |
| `Plans.md` | 任务管理 | 双方 |

---

## 2. 角色分工

### Cursor（PM）的职责

- [ ] 任务的定义和添加到 Plans.md
- [ ] 向 Claude Code 委托工作
- [ ] 完成报告的审查
- [ ] 生产部署的判断和执行

### Claude Code（Worker）的职责

- [ ] 任务的实现
- [ ] 测试的执行
- [ ] 提交 & 推送
- [ ] CI 失败时的修复（最多3次）
- [ ] staging 部署（如适用）
- [ ] 完成报告的创建

---

## 3. 任务管理（Plans.md）

### 标记一览

| 标记 | 含义 | 设置者 |
|---------|------|--------|
| `pm:依頼中` | PM 委托（兼容: cursor:依頼中） | PM（Cursor/PM Claude） |
| `cc:TODO` | Claude Code 未开始 | 任意一方 |
| `cc:WIP` | Claude Code 工作中 | Claude Code |
| `cc:完了` | Claude Code 完成 | Claude Code |
| `pm:確認済` | PM 确认完成（兼容: cursor:確認済） | PM（Cursor/PM Claude） |
| `cursor:依頼中` | （兼容）与 pm:依頼中 同义 | Cursor |
| `cursor:確認済` | （兼容）与 pm:確認済 同义 | Cursor |
| `blocked` | 阻塞中 | 任意一方 |

### 状态转换

```
pm:依頼中 → cc:WIP → cc:完了 → pm:確認済
```

---

## 4. CI/CD 规则

### Claude Code 的应对范围

- ✅ CI 错误的自动修复（最多3次）
- ✅ staging 环境的部署
- ❌ 禁止生产部署（Cursor 的职责）

### 3次规则

CI 连续失败3次时：
1. 中止自动修复
2. 创建升级报告
3. 交由 Cursor 判断

---

## 5. 提交信息规范

```
feat: 新功能
fix: 错误修复
docs: 文档
refactor: 重构
test: 测试
chore: 其他
```

例: `feat: 添加用户认证功能`

---

## 6. 禁止事项

### Claude Code 的禁止事项

- ❌ 直接部署到生产环境
- ❌ 4次以上的 CI 自动修复
- ❌ 范围外的工作（未被委托的功能添加等）
- ❌ 安全相关的设置更改（API 密钥、认证等）

### 共同的禁止事项

- ❌ force push（`--force` / `--force-with-lease`）
- ❌ 提交敏感信息

> **Note**: 单独开发项目中允许直接 push 到 main。CI 会在 push to main 时执行验证，因此质量门得以维持。有共同开发者时请切换为必须 PR。

---

## 7. 故障排除

### 常见问题

| 症状 | 处理方法 |
|------|--------|
| CI 持续失败 | 3次尝试后，上报给 Cursor |
| 任务不明确 | 向 Cursor 请求确认 |
| Plans.md 未更新 | 使用 `/sync-status` 确认状态 |

---

*本文档供 Cursor 和 Claude Code 双方参考。*
