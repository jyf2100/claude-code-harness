---
_harness_template: "Plans.md.template"
_harness_version: "2.5.27"
---

# Plans.md - 任务管理

> **项目**: {{PROJECT_NAME}}
> **最后更新**: {{DATE}}
> **更新者**: Claude Code

---

## 🔴 进行中的任务

<!-- cc:WIP 的任务记载在这里 -->

（当前无）

---

## 🟡 未开始的任务

<!-- cc:TODO, pm:依頼中（兼容: cursor:依頼中） 的任务记载在这里 -->

- [ ] 项目初始设置 `cc:TODO`
  - 创建基本目录结构
  - 安装依赖关系

---

## 🟢 完成的任务

<!-- cc:完了, pm:確認済（兼容: cursor:確認済） 的任务记载在这里 -->

- [x] 2-Agent / 2-Claude 工作流导入 `pm:確認済` ({{DATE}})

---

## 📦 归档

<!-- 旧的完成任务移动到这里 -->

---

## 标记图例

| 标记 | 含义 |
|---------|------|
| `pm:依頼中` | PM 委托的任务（单独PM Claude / Cursor PM 共通） |
| `cc:TODO` | Claude Code 未开始 |
| `cc:WIP` | Claude Code 工作中 |
| `cc:完了` | Claude Code 完成（待确认） |
| `pm:確認済` | PM 确认完成 |
| `cursor:依頼中` | （兼容）与 `pm:依頼中` 同义 |
| `cursor:確認済` | （兼容）与 `pm:確認済` 同义 |
| `blocked` | 阻塞中（附带理由） |

---

## 扩展记法（可选）

大规模任务的管理中，可以**选择性**使用以下记法。

### 任务 ID / 依赖关系 / 可并行

```markdown
- [ ] T001: 认证功能 `cc:TODO`
- [ ] T002: 用户API `cc:TODO` depends:T001
- [ ] T003: 商品API `cc:TODO` [P]
- [ ] T004: 订单API `cc:TODO` depends:T001,T003
```

| 记法 | 含义 | 例 |
|------|------|-----|
| `T001:` | 任务ID（可选） | 用于引用和依赖指定 |
| `depends:ID` | 依赖任务 | `depends:T001,T002` |
| `[P]` | 可并行（Parallelizable） | 可与其他任务同时执行 |

**注意**: 这些是可选的。传统格式（无 ID/depends/[P]）也能正常工作。

---

## 最后更新信息

- **更新日期**: {{DATE}}
- **最后会话负责人**: Claude Code
- **分支**: main
