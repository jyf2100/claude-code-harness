---
description: Memory Integration 规则 - 通过与 Claude-mem 集成实现跨会话质量强化
paths: "**"
_harness_template: "rules/memory-integration.md.template"
_harness_version: "{{VERSION}}"
_harness_condition: "claude_mem.enabled"
---

# Memory Integration

本项目已启用 **Claude-mem 集成**。

通过跨会话的质量/上下文维持功能，可以活用过去的学习成果。

---

## 功能概述

| 功能 | 说明 |
|------|------|
| 过去记忆搜索 | 用 `mem-search` 技能搜索过去的工作历史 |
| 护栏历史 | 参照过去的测试篡改防止记录 |
| 上下文延续 | 从上次会话状态立即继续 |
| 模式学习 | 复用过去的 bug 修复/设计决策 |

---

## 在技能中的使用方法

### 1. 会话开始时（session-init）

会话开始时自动显示过去的上下文：

```
🚀 会话开始

📚 过去的上下文:
- 上次: Feature X 设计完成（采用 RBAC）
- 护栏触发: 过去 2 次防止了测试篡改

📋 继续任务:
- Feature X 实现阶段
```

### 2. 实现时（impl 技能）

实现前自动搜索过去的相关模式：

```
# 例: 实现 API 端点时
mem-search: "API endpoint pattern" "error handling"

# 显示过去的实现模式、陷阱
```

### 3. 审查时（harness-review 技能）

参照过去的类似审查意见：

```
# 例: 审查认证代码时
mem-search: "authentication review" "security"

# 显示过去指出的认证相关问题模式
```

### 4. 验证时（verify 技能）

参照过去的构建错误/解决方案：

```
# 例: 发生构建错误时
mem-search: "build error" "CORS" "解决"

# 显示过去的类似错误和解决方法
```

---

## 记录的内容

### Harness 模式下自动记录

| 观察类型 | 说明 | 例 |
|-----------|------|-----|
| `plan` | 向 Plans.md 添加/更新任务 | "将 Feature X 分成 3 个任务" |
| `guard` | 护栏触发 | "阻止添加 it.skip()" |
| `decision` | 设计决策和理由 | "采用 RBAC: 重视可扩展性" |
| `bugfix` | Bug 修复内容 | "CORS 错误: 添加 Allow-Origin" |
| `handoff` | PM ↔ Impl 角色切换 | "PM → Impl: 请求实现认证功能" |

### 观察概念

| 概念 | 搜索关键词例 |
|-----------|-----------------|
| `user-intent` | "用户想要什么" |
| `gotcha` | "陷阱"、"注意点" |
| `pattern` | "可复用模式" |
| `test-quality` | "测试篡改"、"质量" |

---

## 手动搜索

手动搜索过去的记忆时：

```
# 使用 mem-search 技能
用 Skill 工具调用 'mem-search'

# 搜索查询例
"认证的实现模式"
"CORS 错误 解决"
"测试篡改 防止"
"上次会话做了什么？"
```

---

## 护栏强化

过去的护栏触发历史会被累积学习：

### 例: 测试篡改防止

```
⚠️ 此项目过去阻止了以下操作：

- 2024-01-15: 阻止添加 it.skip()（支付测试）
- 2024-01-20: 阻止放宽 expect.toEqual()

💡 禁止篡改测试。
   请修复实现以通过测试。
```

---

## 与 SSOT 的集成

重要的观察可以提升到 SSOT（decisions.md / patterns.md）：

```bash
# 将重要决策提升到 SSOT
/memory sync

# 提升对象:
# - decision 类型的观察 → decisions.md
# - pattern 概念的观察 → patterns.md
```

---

## Token 成本

Claude-mem 集成会产生每会话约 **+500〜1,000 token** 的额外成本。

### 成本效益

| 观点 | 优势 |
|------|---------|
| 热启动 | 减少上下文重建工作（节省数千 token） |
| 防止重复 | 避免重复相同调查 |
| 质量累积 | 不重复过去的错误 |

---

## 禁用

临时禁用 Claude-mem 集成时：

```bash
# 编辑 settings.json
# 将 "CLAUDE_MEM_MODE" 改为 "code"
```

---

## 相关

- `/setup harness-mem` - Claude-mem 集成设置
- `mem-search` 技能 - 过去记忆搜索
- `/memory sync` - 观察提升到 SSOT
