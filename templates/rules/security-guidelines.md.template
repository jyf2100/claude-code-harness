# 安全指南

> 此规则由 Claude harness 自动生成。
> 目的: 检测需要安全注意的变更，并提供检查清单。

## 需要安全注意的模式

### 文件路径判定

| 路径模式 | 注意级别 | 理由 |
|-------------|-----------|------|
| `src/auth/`, `auth/` | 🔴 高 | 认证逻辑 |
| `src/api/`, `api/`, `routes/` | 🟠 中 | API 端点 |
| `middleware/` | 🟠 中 | 请求处理 |
| `src/payment/`, `billing/` | 🔴 高 | 支付处理 |
| `config/`, `.env*` | 🟠 中 | 敏感配置 |

### 代码变更模式

| 模式 | 注意级别 | 理由 |
|---------|-----------|------|
| `password`, `secret`, `token` | 🔴 高 | 认证信息 |
| 动态代码评估函数 | 🔴 高 | 任意代码执行风险 |
| SQL 查询字符串拼接 | 🔴 高 | SQL 注入 |
| React 原生 HTML 插入 | 🟠 中 | XSS 风险 |
| `cors()` 配置变更 | 🟠 中 | 跨域 |

## OWASP Top 10 对应检查清单

### 认证/授权变更时

- [ ] **A01: 访问控制缺陷**
  - 所有端点是否都有授权检查
  - 基于角色的访问控制是否正确

- [ ] **A02: 加密失败**
  - 密码是否已哈希化（bcrypt, argon2）
  - 敏感数据是否已加密
  - 是否强制使用 HTTPS

- [ ] **A07: 认证失败**
  - 暴力破解对策（速率限制）
  - 会话管理是否适当
  - 是否考虑了 MFA 支持

### API 变更时

- [ ] **A03: 注入**
  - 输入值是否已清理
  - 是否使用参数化查询
  - 命令注入对策

- [ ] **A04: 不安全设计**
  - 最小权限原则
  - 输入验证是否在业务逻辑层面实现

- [ ] **A05: 安全配置错误**
  - 是否公开了不必要的端点
  - 错误消息是否过于详细
  - 默认凭据是否已更改

### 依赖关系变更时

- [ ] **A06: 易受攻击和过时的组件**
  - 添加的包是否有已知漏洞（`npm audit`）
  - 版本是否最新
  - 是否在维护中

## 面向技能的判定逻辑

```
检测文件变更
    ↓
路径模式匹配
    ↓
├── auth/, api/, payment/ → 显示安全检查清单
├── middleware/ → CORS、授权检查
├── .env*, config/ → 敏感信息泄露检查
└── 其他 → 常规处理
```

## 提议模板

**认证相关变更时**:
> ⚠️ 检测到认证相关的代码变更。
> 请确认安全检查清单：
> - [ ] 密码哈希化
> - [ ] 会话管理
> - [ ] 速率限制

**API 变更时**:
> ⚠️ 检测到 API 端点变更。
> - [ ] 输入值验证
> - [ ] 授权检查
> - [ ] 错误处理

## 面向 VibeCoder（换一种说法）

| 技术术语 | 面向 VibeCoder |
|---------|---------------|
| 认证 | 登录/密码相关 |
| 授权 | 控制谁能做什么 |
| 清理 | 将危险字符无害化 |
| 注入 | 恶意代码注入 |

**面向 VibeCoder 的提议**:
> 🔐 这是与登录相关的变更。
> 建议让安全专家审查。
> 至少确认以下内容：
> - 密码是否以明文存储
> - 是否有登录尝试次数限制

## 注意事项

- **警告只是建议**: 最终由用户判断
- **允许误报**: 向安全方向倾斜
- **建议专家审查**: 重要变更建议人工审查
