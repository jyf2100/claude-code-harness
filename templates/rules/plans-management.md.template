---
description: Plans.md 任务管理规则（仅在编辑 Plans.md 时适用）
paths: "**/Plans.md"
_harness_template: "rules/plans-management.md.template"
_harness_version: "2.5.27"
---

# Plans.md Management Rules

## 任务描述格式

```markdown
- [ ] 任务说明 `标记`
  - 子任务1
  - 子任务2
```

## 标记运营

| 标记 | 添加者 | 含义 |
|---------|--------|------|
| `pm:依頼中` | PM（Cursor/PM Claude） | 请求任务 |
| `cc:TODO` | Claude Code | 未开始 |
| `cc:WIP` | Claude Code | 工作中 |
| `cc:完了` | Claude Code | 完成 |
| `pm:確認済` | PM（Cursor/PM Claude） | 审查完成 |
| `cursor:依頼中` | Cursor | （兼容）与 `pm:依頼中` 同义 |
| `cursor:確認済` | Cursor | （兼容）与 `pm:確認済` 同义 |

## 章节构成

```markdown
## 🔴 进行中的任务
（cc:WIP 的任务）

## 🟡 未开始的任务
（cc:TODO, pm:依頼中（兼容: cursor:依頼中） 的任务）

## 🟢 完成的任务
（cc:完了, pm:確認済（兼容: cursor:確認済） 的任务）

## 📦 归档
（旧的完成任务）
```

## 更新规则

1. **即时更新**: 任务开始时立即添加 `cc:WIP`，完成时立即添加 `cc:完了`
2. **记录摘要**: 完成时追加工作内容摘要
3. **记录日期**: 完成章节记录日期 `(YYYY-MM-DD)`
4. **归档**: 7天前的完成任务移动到📦归档

## 禁止事项

- ❌ 擅自更改他人的标记
- ❌ 删除进行中的任务
- ❌ 没有摘要就标记完成

---

## 扩展记法（可选）

大型项目可**选择**使用以下记法：

```markdown
- [ ] T001: 认证功能 `cc:TODO`
- [ ] T002: 用户API `cc:TODO` depends:T001
- [ ] T003: 商品API `cc:TODO` [P]
```

| 记法 | 含义 |
|------|------|
| `T001:` | 任务ID（用于依赖指定） |
| `depends:ID` | 依赖任务（可逗号分隔） |
| `[P]` | 可并行执行（Parallelizable） |

**向后兼容**: 没有这些也可以照常工作。
