---
description: Skills Gate 规则 - 代码编辑前促进技能使用
paths: "**/*.{ts,tsx,js,jsx,py,rb,go,rs,java,kt,swift,c,cpp,h,hpp,cs,php}"
_harness_template: "rules/skills-gate.md.template"
_harness_version: "{{VERSION}}"
_harness_condition: "skills_gate.enabled"
---

# Skills Gate

本项目已启用 **Skills Gate**。

## 原则

**在进行代码编辑（Write/Edit）之前，请使用适当的技能。**

技能提供提升工作质量的指南。

---

## 使用技能的时机

| 工作 | 技能 | 调用方式 |
|------|--------|-----------|
| 实现功能 | `impl` | 用 Skill 工具调用"impl" |
| 审查代码 | `harness-review` | 用 Skill 工具调用"harness-review" |
| 创建 UI | `ui` | 用 Skill 工具调用"ui" |
| 实现认证/支付 | `auth` | 用 Skill 工具调用"auth" |
| 配置部署 | `deploy` | 用 Skill 工具调用"deploy" |

---

## 为什么要使用技能

### 1. 提升质量

技能包含该工作所需的检查清单和最佳实践。
可以防止遗漏，保持一致的质量。

### 2. 高效工作

技能提供工作的标准流程。
明确应该从什么开始，应该确认什么。

### 3. 知识积累

通过技能可以访问项目特有的模式和决策。

---

## 豁免（不需要技能）

以下文件不适用 Skills Gate：

| 模式 | 理由 |
|---------|------|
| `*.md`, `*.txt` | 文档 |
| `*.json` | 配置文件 |
| `.claude/*` | harness 内部文件 |
| `docs/*`, `templates/*` | 文档/模板 |

---

## 工作原理

```
尝试编辑代码
    ↓
本次会话是否使用过技能？
    ├── YES → 通过（可编辑）
    └── NO  → 阻止（促进使用技能）
```

- **只要使用过1次技能**，该会话期间就会解除限制
- 技能使用由 `session-skills-used.json` 跟踪

---

## 被阻止时

不使用技能尝试编辑代码时，会显示以下消息：

```
[Skills Gate] 请在编辑代码前使用技能。

可用技能: impl, review, ui, ...

例: 用 Skill 工具调用 'impl' 或 'review'
```

这种情况下，请先用 Skill 工具调用适当的技能，然后再次尝试编辑。

---

## 如何选择技能

### 不确定就用 `impl`

`impl` 技能提供通用的实现指南。
不知道该用什么时，先调用 `impl`。

### 使用多个技能

一项工作可以使用多个技能。
例: 实现 UI 组件时 → `impl` + `ui`

---

## 更改设置

Skills Gate 的启用/禁用由 `.claude/state/skills-config.json` 管理。

直接编辑 `.claude/state/skills-config.json`，或用 `/setup` 技能重新设置。

```json
// 禁用（不推荐）
{ "skills_gate_enabled": false }

// 添加技能
{ "active_skills": ["impl", "auth"] }
```

---

## Memory-Enhanced Skills（Claude-mem 集成时）

当 Claude-mem 启用时，技能会利用过去的记忆来提升质量。

### 获得增强的技能

| 技能 | 增强内容 |
|--------|---------|
| `impl` | 自动参照过去的实现模式/陷阱 |
| `harness-review` | 参照过去的类似审查意见 |
| `verify` | 参照过去的构建错误解决方案 |
| `session-init` | 显示护栏触发历史/工作摘要 |

### 使用示例

```
# 使用 impl 技能时
📚 过去的相关模式:
- API 端点: 采用 RESTful 设计模式
- 错误处理: 使用统一异常处理器

💡 过去的陷阱:
- CORS 设置: 必须有 Allow-Origin 头
```

### 无 Claude-mem 环境

未设置 Claude-mem 时，各技能照常工作。
Memory-Enhanced 功能会自动跳过。

### 设置

```bash
# 启用 Claude-mem 集成
/setup harness-mem
```
