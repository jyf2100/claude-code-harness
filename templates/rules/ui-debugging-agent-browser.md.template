---
description: UI调试/验证优先使用 agent-browser（如已安装）
alwaysApply: true
_harness_template: "rules/ui-debugging-agent-browser.md.template"
_harness_version: "2.7.13"
---

# UI Debugging Rule (Prefer Agent Browser)

## 基本方针

- UI/UX缺陷、需要屏幕重现的调查，**优先使用 agent-browser**
- 优先于其他浏览器工具（MCP 的 chrome-devtools、playwright）尝试 agent-browser
- 在界面和源代码之间往返，**重现 → 推断原因 → 修复 → 再验证** 以短周期循环

## agent-browser 的优势

| 特性 | 说明 |
|------|------|
| **AI 优化** | `snapshot -i -c` 可获取元素引用（`@e1`, `@e2`） |
| **简洁** | CLI 直接执行，无需 MCP |
| **高速** | Rust 构建的二进制 |
| **会话管理** | `--session` 可并行管理多个标签页 |

## 推进方式（推荐）

### 1. 确定重现条件

- 明确目标 URL、用户状态（登录/权限/数据）、期望值/实际行为

### 2. 用 agent-browser 重现

```bash
# 打开页面
agent-browser open https://example.com/target-page

# 获取 AI 专用快照（仅交互元素）
agent-browser snapshot -i -c

# 输出示例:
# - button "Login" [ref=e1]
# - input "Email" [ref=e2]
# - input "Password" [ref=e3]
```

### 3. 执行操作重现问题

```bash
# 通过元素引用点击
agent-browser click @e1

# 填写表单
agent-browser fill @e2 "test@example.com"
agent-browser fill @e3 "password123"

# 确认状态
agent-browser snapshot -i -c
```

### 4. 观察→假设→确认代码

- 以快照/截图/控制台日志信息为依据，缩小原因候选项
- 确认相关源代码（UI/状态管理/API/验证）

### 5. 用最小修改修复

- 控制修改范围，确保能说明意图和副作用

### 6. 用 agent-browser 再验证

```bash
# 确认同样步骤不会重现
agent-browser open https://example.com/target-page
agent-browser snapshot -i -c
agent-browser click @e1
# ...
```

## agent-browser 不可用时的回退方案

按以下顺序尝试替代方案：

1. **MCP 浏览器工具**（chrome-devtools, playwright）
2. **手动重现**:
   - 重现步骤（URL/操作/期望值/实际）
   - 截图/视频
   - 控制台日志/网络日志
3. **自动 E2E**:
   - 如可能添加最小化的 Playwright/Cypress 测试以防止回归

## 常用命令

```bash
# 基本操作
agent-browser open <url>          # 打开页面
agent-browser snapshot -i -c      # 快照（AI 专用）
agent-browser click @e1           # 点击
agent-browser fill @e2 "text"     # 输入
agent-browser screenshot out.png  # 截图

# 调试
agent-browser --headed open <url> # 显示浏览器
agent-browser console             # 显示控制台日志
agent-browser errors              # 显示页面错误

# 信息获取
agent-browser get text @e1        # 获取文本
agent-browser get html @e1        # 获取 HTML
agent-browser get url             # 获取当前 URL
```
