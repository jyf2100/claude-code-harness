# TDD 指南

> 此规则由 Claude harness 自动生成。
> 目的: 在适当场景下提议 TDD（测试驱动开发），帮助提升质量。

## 什么是 TDD

**Test-Driven Development（测试驱动开发）**:
1. **Red**: 先写失败的测试
2. **Green**: 编写让测试通过的最小实现
3. **Refactor**: 整理代码

## 适用标准

### 推荐 TDD（测试先行）

| 条件 | 理由 |
|------|------|
| Plans.md 中有 `[feature]` 标签 | 新功能用测试明确规范 |
| `src/core/`, `src/services/` 下 | 业务逻辑质量重要 |
| 添加 API 端点 | 先定义契约（接口） |
| 复杂算法 | 先梳理边缘情况 |

### 重现测试先行

| 条件 | 理由 |
|------|------|
| Plans.md 中有 `[bugfix]` 标签 | 用重现 bug 的测试验证修复 |
| "不工作"类报告 | 确定问题本质 |

### 不需要 TDD

| 条件 | 理由 |
|------|------|
| `[config]`, `[docs]` 标签 | 设置和文档不是测试对象 |
| UI 样式调整 | 视觉难以测试 |
| 配置文件变更 | 不需要验证 |
| 已有测试覆盖 | 避免重复 |

## 工作流

### 面向工程师

```
1. 创建测试文件
   - 用 describe/it 描述期望行为
   - 还没有实现所以会失败（Red）

2. 最小实现
   - 只写让测试通过的代码（Green）
   - 不追求完美

3. 重构
   - 保持测试通过的同时改进
   - 命名、结构、性能
```

### 面向 VibeCoder（换一种说法）

| TDD 术语 | 面向 VibeCoder |
|---------|---------------|
| 先写测试 | 先确定成功标准 |
| Red（失败的测试） | 创建"这样就算 OK"的列表 |
| Green（让测试通过） | 让它最小限度能工作 |
| Refactor（重构） | 整理代码 |

### 提议模板

**面向工程师**:
> 建议用 TDD 方式推进此功能添加。
> 要从测试文件开始创建吗？

**面向 VibeCoder**:
> 先确定"什么情况下算成功"吧。
> 要创建检查清单吗？

## 判定逻辑（用于技能）

```
接收任务
    ↓
确认标签: [feature] / [bugfix] / [config] / [docs]
    ↓
确认文件路径: src/core/, src/services/, src/api/
    ↓
├── [feature] + 业务逻辑 → 推荐 TDD
├── [bugfix] → 重现测试先行
├── [config] / [docs] → 不需要 TDD
└── 仅 UI 样式 → 不需要 TDD
```

## 注意事项

- **不是强制而是建议**: 由用户判断
- **尊重现有测试**: 不重复创建
- **未检测到测试框架时**: 跳过 TDD 建议
