# 质量判定门

> 此规则由 Claude harness 自动生成。
> 目的: 根据任务类型/文件路径自动建议适当的质量标准。

## 质量判定矩阵

| 判定类型 | 触发条件 | 对象技能 | 建议内容 |
|-----------|---------|-----------|---------|
| **TDD** | [feature] + 业务逻辑 | impl, verify | 建议测试先行 |
| **安全** | auth/, api/, payment/ | impl, harness-review, auth | 显示检查清单 |
| **覆盖率** | 检测到无测试/不足 | harness-review | 指出需要添加测试 |
| **a11y** | UI 组件 | ui, harness-review | 无障碍检查 |
| **性能** | DB 查询, 循环 | impl, harness-review | 优化警告 |

## 判定流程

```
接收任务
    ↓
┌─────────────────────────────────────────┐
│           质量判定门                     │
├─────────────────────────────────────────┤
│  输入:                                  │
│  - Plans.md 的标签: [feature]/[bugfix]  │
│  - 目标文件路径                          │
│  - 变更内容的关键词                      │
│                                         │
│  判定:                                  │
│  ├── 是否推荐 TDD？                     │
│  ├── 是否需要注意安全？                  │
│  ├── 是否需要注意性能？                  │
│  ├── 是否需要注意 a11y？                │
│  └── 是否覆盖率不足？                   │
│                                         │
│  输出: 建议相应的判定                    │
└─────────────────────────────────────────┘
          ↓
    各技能/命令显示建议
```

## 判定标准详情

### TDD 判定

| 条件 | TDD 推荐度 |
|------|-----------|
| [feature] + src/core/ | ★★★ 强烈推荐 |
| [feature] + src/services/ | ★★★ 强烈推荐 |
| [feature] + src/api/ | ★★☆ 推荐 |
| [bugfix] | ★★☆ 推荐重现测试 |
| [config], [docs] | ☆☆☆ 不需要 |

### 安全判定

| 路径 | 注意级别 |
|------|-----------|
| auth/, login/, session/ | 🔴 高 |
| api/, routes/, middleware/ | 🟠 中 |
| payment/, billing/, stripe/ | 🔴 高 |
| config/, .env | 🟠 中 |

### a11y 判定

| 路径 | 检查项目 |
|------|------------|
| src/components/ | alt, aria, 键盘操作 |
| src/app/, src/pages/ | 标题结构, 焦点管理 |
| 使用 UI 库 | 确认组件的 a11y 支持 |

### 性能判定

| 模式 | 警告内容 |
|---------|---------|
| `SELECT * FROM` + 循环内 | 可能是 N+1 查询 |
| 大数组的 `.map().filter()` | 多次遍历 |
| `useEffect` 内的繁重处理 | 渲染延迟 |
| 直接加载图片 | 考虑优化 |

## 提议模板

### 面向工程师

```markdown
## 🎯 质量判定结果

对此任务建议以下质量标准：

| 判定 | 推荐度 | 理由 |
|------|--------|------|
| TDD | ★★★ | [feature] 标签 + 业务逻辑 |
| 安全 | ★★☆ | API 端点变更 |

### 推荐操作
1. 先创建测试文件
2. 确认安全检查清单

继续吗？
```

### 面向 VibeCoder

```markdown
## 🎯 这项工作需要注意的事项

1. **先确定成功标准**
   - 将"什么情况下算 OK"列成清单

2. **安全确认**
   - 因为涉及登录相关，让专家也看一下会更安心

请选择推进方式：
1. 从成功标准开始（推荐）
2. 先开始做再说
```

## 各技能的使用方法

### impl 技能

```
步骤 0: 质量判定门
    ↓
显示判定结果
    ↓
根据用户选择开始实现
```

### harness-review 技能

```
开始审查
    ↓
用质量判定门确定重点领域
    ↓
├── 有安全判定 → 安全重点审查
├── 覆盖率不足 → 指出需要添加测试
└── 有 a11y 判定 → 无障碍检查
```

### verify 技能

```
错误报告
    ↓
TDD 判定（是否推荐重现测试？）
    ↓
├── Bug 报告 → 建议重现测试
└── 测试失败 → 判断是测试还是实现的问题
```

## 设置

### 判定的启用/禁用

`.claude-code-harness.config.yaml`:

```yaml
quality_gates:
  tdd:
    enabled: true
    auto_propose: true  # 自动建议
  security:
    enabled: true
    level: medium  # low / medium / high
  a11y:
    enabled: true
  performance:
    enabled: true
```

### 判定的自定义

```yaml
quality_gates:
  tdd:
    paths:
      high_priority:
        - src/core/**
        - src/domain/**
      exclude:
        - src/utils/**
```

## 注意事项

- **是建议而非强制**: 由用户最终判断
- **允许误报**: 向安全方向倾斜
- **同时显示多个判定**: 显示所有适用的判定
- **学习与改进**: 将反馈记录到 SSOT 以改进
